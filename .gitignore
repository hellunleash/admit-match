node_modules/
.env
.env.local
dist/
*.log
.cache/

# Personal academic data. Transcripts contain grades, failures and identifying detail —
# they never belong in a public repository. Only profile.example.json is tracked.
profile.local.json
*.local.json
transcripts/
